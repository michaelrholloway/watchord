[package]
name = "watchord-theory"
description = "watchord annotations: voicing, inversion, slash, upper structure, staff — pure functions over an analysis, never the engine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
watchord-core = { workspace = true, features = ["serde"] }
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
