[package]
name = "watchord-model"
description = "watchord app model: the UI-independent state machine, and the fakes for its seams"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
watchord-core = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
uuid = { workspace = true }
